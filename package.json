{
  "name": "summarizer",
  "version": "1.0.0",
  "description": "Build and deploy tooling for the book summaries site",
  "scripts": {
    "build-search": "node scripts/build-search-index.mjs",
    "vercel-build": "node scripts/vercel-build.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
